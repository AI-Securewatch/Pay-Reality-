
  # Make Buttons Clickable

  This is a code bundle for Make Buttons Clickable. The original project is available at https://www.figma.com/design/hF6wE2H6ECjxjWgxjpyIs7/Make-Buttons-Clickable.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  